# project06demo03example03

This project demonstrates a pattern:

events → state → actions → event

Each SQL file is a standalone domain that follows this structure.

Run any file in Oracle APEX SQL Workshop and:
- query *_state_v
- query *_actions_v
- call procedures

Observe that only valid actions are exposed.
